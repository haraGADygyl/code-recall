# Default LLM provider: "openai" or "ollama"
DEFAULT_PROVIDER="openai"

# Default question mode: "advanced-python", "rest-api", "fastapi", or "system-design"
DEFAULT_QUESTION_MODE="system-design"

# OpenAI Configuration
OPENAI_API_KEY=""
OPENAI_MODEL_NAME="gpt-4.1-mini"
OPENAI_TIMEOUT_SECONDS="45"
OPENAI_MAX_RETRIES="1"

# Ollama Configuration
MODEL_NAME="gemma2:2b"
OLLAMA_TIMEOUT_SECONDS="120"
OLLAMA_PROBE_TIMEOUT_SECONDS="5"
OLLAMA_KEEP_ALIVE="0"

# Attempts allowed to stop the correct answer from being the longest option.
# 1 disables regeneration; each extra attempt costs one more request.
ANSWER_BALANCE_ATTEMPTS="2"

# Optional directories to prepend to PATH in recall.sh
# EXTRA_PATH="/custom/bin"
